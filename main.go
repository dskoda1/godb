package main

import (
  // "fmt"
  // "time"

)


func main() {
  // db := getDb("fantasy-football_development")
  // rows, err := db.Query("SELECT * FROM players")
  // if err != nil {
  //   panic(err)
  // }
  // defer rows.Close()
  //
  // var (
  //   id int
  //   name string
  //   position_id int
  //   created_at time.Time
  //   updated_at time.Time
  // )
  //
  // for rows.Next() {
	//    err := rows.Scan(&id, &name, &position_id, &created_at, &updated_at)
	//    if err != nil {
	// 	     panic(err)
	//    }
	//    fmt.Println(id, name, position_id, created_at, updated_at)
  //  }
}
